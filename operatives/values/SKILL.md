---
name: values
version: 0.1.0
description: |
  Inject values into a system with enough precision for the machine to honour them.
effort: "high"
argument-hint: "describe the values you want to inject or the system that needs them"
voice-triggers:
  - "values"
  - "values layer"
  - "inject values"
  - "what matters"
---

# /values — Inject Values Into the System

Read and follow ${CLAUDE_SKILL_DIR}/CLAUDE.md.
Start by checking for OPERATIVES.md in the current working directory.
Follow the Session Start Protocol in CLAUDE.md.
